@{
    ViewBag.Title = "SellDate";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@using Webdiyer.WebControls.Mvc
@using _1Fix.Entity
@model PagedList<SellBaseInfo>
<div class="pageheader">
    <h2><i class="fa fa-suitcase"></i> 审核寄卖信息 <span>Subtitle goes here...</span></h2>
    <div class="breadcrumb-wrapper">
        <span class="label">您的位置:</span>
        <ol class="breadcrumb">
            <li><a href="/Consignment/Index">寄卖管理</a></li>
            <li class="active">寄卖用户查询</li>
        </ol>
    </div>
</div>
<div class="contentpanel">
    <div class="row">
        <div class="col-md-12">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <div class="panel-btns">
                        @*<a class="panel-close" href="">×</a>*@
                        <a class="minimize" href="">−</a>
                    </div>
                    <h4 class="panel-title">高级查询</h4>
                    @*<p>This is an example of a form using a placeholder instead of label.</p>*@
                </div>
                @using (Ajax.BeginForm("SellBaseInfo", "", new AjaxOptions { HttpMethod = "Get", UpdateTargetId = "divList" }, new { id = "divForm" }))
                {
                    <div class="panel-body">
                        <div class="row row-pad-5">
                            <div class="col-xs-3">
                                <input type="text" class="form-control" placeholder="寄卖人" name="contactname">
                            </div>
                            <div class="col-xs-3">
                                <input type="text" class="form-control" placeholder="手机品牌" name="contactphone">
                            </div>
                            <div class="col-xs-3">
                                <input type="text" class="form-control" placeholder="IMEI" name="sn">
                            </div>
                            <div class="col-xs-3">
                                <input type="text" class="form-control" placeholder="快递单号" name="CourierNo">
                            </div>
                            
                        </div><!-- row -->
                        <div class="row row-pad-5" style=" text-align:center;margin-top:10px;">
                            <div class="col-xs-12">
                                <button type="submit" class="btn btn-primary">搜索</button>
                            </div>
                        </div>
                    </div><!-- panel-body -->
                    <div class="panel-footer">
                        @*<button type="submit" class="btn btn-primary">搜索</button>*@
                    </div>
                }
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-md-12">
            <div id="divList" class="table-responsive">
                @Html.Partial("_SellBaseInfoList", Model)
            </div><!-- table-responsive -->
        </div>
    </div>
</div>
@{Html.RegisterMvcPagerScriptResource();}